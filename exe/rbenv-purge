#!/usr/bin/env ruby

system "rbenv global system"

devs = %x[rbenv versions].split.select{|v| v =~ /[0-9|dev]$/ }

devs.each do |dev|
  system "rbenv uninstall -f #{dev} && rbenv install -f #{dev}"
end

dev = devs.find{|v| v =~ /dev/ }

system "rbenv global #{dev} && rbenv shell --unset"
