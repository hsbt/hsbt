hosted_zone "hsbt.org." do
  rrset "hsbt.org.", "A" do
    dns_name "fourier.hsbt.org."
  end

  rrset "anonymous.hsbt.org.", "CNAME" do
    ttl 300
    resource_records(
      "tdiary.herokuapp.com"
    )
  end

  rrset "bakagaiku.hsbt.org.", "CNAME" do
    ttl 300
    resource_records(
      "bakagaiku.herokuapp.com"
    )
  end

  rrset "ci.hsbt.org.", "A" do
    dns_name "fourier.hsbt.org."
  end

  rrset "fourier.hsbt.org.", "A" do
    ttl 300
    resource_records(
      "133.242.17.37"
    )
  end

  rrset "irc.hsbt.org.", "A" do
    dns_name "fourier.hsbt.org."
  end

  rrset "munin.hsbt.org.", "A" do
    dns_name "fourier.hsbt.org."
  end

  rrset "tumblr.hsbt.org.", "CNAME" do
    ttl 300
    resource_records(
      "domains.tumblr.com"
    )
  end

  rrset "www.hsbt.org.", "A" do
    dns_name "fourier.hsbt.org."
  end
end
