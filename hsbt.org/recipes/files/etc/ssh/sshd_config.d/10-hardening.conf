# Named below 50 so it wins over a future cloud-init drop-in
# (sshd uses first-match and includes this directory before the main config).
PasswordAuthentication no
PermitRootLogin no
AllowUsers ubuntu
X11Forwarding no
MaxAuthTries 3
LoginGraceTime 30
