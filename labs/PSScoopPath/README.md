# PSScoopPath

Small PowerShell module for resolving Scoop-related paths.

## Development

- Functions live under `Public/` and `Private/`.
- Public functions are exported automatically by `PSScoopPath.psm1`.
