before_script:
  - cp config/database.yml.sample config/database.yml
  - TRAVIS_INIT=1 bundle exec rake db:create db:migrate
  - rm -r solr
  - RAILS_ENV=test bundle exec rake sunspot:solr:start

rvm:
  - 1.9.3
  - ruby-head

script: bundle exec rake test
